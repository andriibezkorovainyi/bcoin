'use strict';

process.title = 'bcoin';
const assert = require('assert');
const Neutrino = require('../lib/wallet/neutrinoclient');

// Doubt in db
const client = new Neutrino({
    file: true,
    argv: true,
    env: true,
    logFile: true,
    logConsole: true,
    logLevel: 'debug',
    db: 'leveldb',
    memory: false,
    persistent: true,
    workers: true,
    listen: true,
    loader: require
});